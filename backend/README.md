# Backend

This folder contains the API server and analytics logic.

- `app/main.py` — FastAPI app and route definitions
- `app/analytics.py` — KPI calculation, sales trends, daily reporting, daily forecast logic
- `app/models.py` — request/response models if added in future
